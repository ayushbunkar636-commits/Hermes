# TOOLS.md - Local Notes

Backlink pipeline uses scripts under `skills/pipeline/`.

## Telegram group for cards
See `config/telegram_card_config.json`.

## State files
- `state/recent_sites.json` — dedup registry for scanned domains
- `state/projects.json` — niche + project URL pairs (optional manual registry)
